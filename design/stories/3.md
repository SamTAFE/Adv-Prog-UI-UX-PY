# User Story 3

As Doug, I want to the page layout to read top to bottom in a single column so the visual order matches how my screen reader reads

**Non-functional requirements:**
- Must work with Screen readers
- All interactive elements reachable by Tab key
- No horizontal scrolling