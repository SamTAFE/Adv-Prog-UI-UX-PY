# User story 2

As Doug, I want the app to warn me when a timestamp is invalid so that I do not waste time trying to extract a frame that cannot exist.

Acceptance criteria:
- The extract button is disabled when invalid / no timestamp is entered
- The app shows an error for invalid timestamp format
- The app shows an error when timestamp is longer than video duration
